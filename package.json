{
  "name": "auto-user-activity",
  "version": "7.0.0",
  "description": "Fetch and Update the recent user activity of a github user",
  "main": "index.js",
  "keywords": [],
  "author": "Vedansh",
  "license": "MIT",
  "scripts": {
    "format:check": "prettier --check index.js",
    "format": "prettier --write index.js",
    "build": "esbuild index.js --bundle --outfile=dist/index.js --platform=node --sourcemap"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "prettier": "^3.4.2"
  }
}
